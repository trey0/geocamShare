
NOTE: Share2 tries to keep the build/s/tmp directory from growing too
large by automatically removing files that are more than one hour old.
(It checks for stale files whenever it writes a new file.)
